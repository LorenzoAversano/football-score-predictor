export const calculScorePrediction = ({
  teamAData,
  teamBData,
  simlulationCount,
}: {
  teamAData: string; //change type
  teamBData: string;
  simlulationCount: number;
}) => {
  //TODO: implement it
  console.log({ teamAData, teamBData, simlulationCount });
};
